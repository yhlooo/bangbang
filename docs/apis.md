# 接口

- `GET /chat/v1/rooms/{roomUID}` 获取房间信息
- `POST /chat/v1/rooms/{roomUID}/channels` 创建信道（加入房间）
- `GET /chat/v1/rooms/{roomUID}/members` 列出成员
- `GET /chat/v1/rooms/{roomUID}/members/{memberUID}` 获取成员信息

